这个程序主要介绍了用applet在网页上显示抖动的文字。
用户可以看到网页上的文字不断地抖动，文字颜色也不断地变化。
通过这个例子，读者可以学习到如何在网页上设计抖动的文字。


这段代码作用是改变单个字符的X、Y坐标，通过改变单个字符的坐标使得字符看起来是在抖动的。

// 单个字符的X坐标
xpoint += (int) (Math.random() * 10);
 // 单个字符的Y坐标
int ypoint = baseline - (int) (Math.random() * 10);
	
