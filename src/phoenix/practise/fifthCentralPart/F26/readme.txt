这个程序主要介绍了用applet在网页上显示三维文字。
通过这个简单的例子，读者可以学习到怎样在网页上显示三维文字，怎样去改变文字的颜色。


int j = (int) (255 * Math.random()); 
int k = (int) (255 * Math.random());
int l = (int) (255 * Math.random());
这段代码利用Math.random()生成三个随机的整型变量，由于生成渐变颜色
